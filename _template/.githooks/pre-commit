#!/bin/sh
set -e

echo "==> Generating templ views..."
templ generate

echo "==> Formatting Go files..."
unformatted=$(gofmt -l .)
if [ -n "$unformatted" ]; then
	gofmt -w .
	echo "    Formatted: $unformatted"
fi

echo "==> Running go vet..."
go vet ./...
